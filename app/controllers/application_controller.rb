# frozen_string_literal: true

# Application Controller, the parent controller for all other controllers in the application
# This controller is the parent controller for all other controllers in the application
# It is the place to define methods that are common to all controllers
# For example, methods for authentication, authorization, and error handling
class ApplicationController < ActionController::Base
end
